<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trial">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trial">
        <activity android:name=".phonesignin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".demouserpage" />
        <activity android:name=".login" />
        <activity android:name=".googlesignin">
<!--            intentfilter-->
        </activity>
        <activity android:name=".successfulsign" />
        <activity android:name=".signup" />
        <activity android:name=".signupdeetspage" />
        <activity android:name=".otpconfirm" />
        <activity android:name=".MainActivity" />
<!--                intentfilter-->
    </application>

</manifest>